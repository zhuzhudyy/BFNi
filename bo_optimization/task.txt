任务 1：导入绘图与标签依赖
请在 bo_optimization/predict_new_sample.py 文件的顶部导入区域，补充导入 matplotlib.pyplot 库，并从当前目录下的 contextual_bo_model 中导入用于图表中文显示的字典 PROCESS_LABELS_CN。

任务 2：编写核心绘图函数
请在主执行逻辑上方，新增一个名为 plot_new_sample_ice 的函数，具体逻辑要求如下：

输入参数接收：接收优化器对象、新样品的特征字典、刚刚预测出的最优工艺参数字典（best_recipe）、目标晶向列表，以及需要扫描的单一工艺参数（默认设为退火温度 Process_Temp）。

构建固定特征：从新样品特征字典中提取出微观结构特征（Pre_）向量，并根据目标晶向列表构建好 Multi-Hot 编码向量。这两部分在接下来的预测中将被完全“锁死”。

构建滑动网格：根据被扫描工艺参数（如温度）的上下界，生成一个包含 50 个点的均匀线性网格。

构建工艺矩阵：将最优工艺参数字典中的其他参数（如时间、气体流量）也全部“锁死”在最佳值上，只让被扫描的参数使用上一步生成的网格数据。

模型预测：将固定好的微观特征、目标晶向以及工艺矩阵拼接成完整的输入矩阵，经过归一化处理后，输入给模型的预测接口，获取预测的均值（mu）和标准差（sigma）。

绘制图表：使用 matplotlib 绘制一条折线图。X 轴为被扫描的工艺参数，Y 轴为预测的产率。要求画出预测均值的主曲线，并利用标准差画出 95% 置信区间的半透明阴影带。

高亮最优点：在曲线上找到模型推荐的最佳工艺参数所对应的位置，用显眼的红色星号标记出来。最后添加好图例、网格线和中文字体支持的标题，并展示图表（plt.show()）。

任务 3：在主程序中触发绘图逻辑
请滚动到脚本最下方的 if __name__ == "__main__": 逻辑块，找到输出预测完成提示语和模型保存路径的地方。在该位置下方补充以下调用逻辑：

获取刚刚输入的新样品特征（可调用现有的提取特征函数）。

为了防止数据污染，请重新实例化一个 ContextualBayesianOptimizer 对象，并加载刚刚保存好的本地模型文件。

调用在任务 2 中编写的绘图函数，传入新实例化并加载好权重的优化器、新样品特征、最优工艺字典和目标晶向，将被扫描参数指定为退火温度。

任务 4：代码检查
修改完毕后，请自行检查是否有未定义的变量、缩进错误，以及确保图表函数不会阻塞前面最优参数文本的正常终端输出。如果一切就绪，请告诉我修改完成。